apiVersion: v1
kind: Service
metadata:
  name: dispatch-service
  labels:
    app: dispatch-service
spec:
  type: ClusterIP
  selector:
    app: dispatch-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

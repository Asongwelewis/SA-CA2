apiVersion: v1
kind: Service
metadata:
  name: sos-service
  labels:
    app: sos-service
spec:
  type: ClusterIP
  selector:
    app: sos-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
